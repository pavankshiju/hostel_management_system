name: hostel_2
description: A new Flutter hostel management project with Firebase integration.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Core Firebase functionality
  firebase_core: ^3.15.2
  # Cloud Firestore for real-time database
  cloud_firestore: ^5.6.2
  
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true